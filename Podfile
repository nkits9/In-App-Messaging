platform :ios, '9.0'

target 'In App Messaging' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for In App Messaging
    pod 'Firebase'
    pod 'Firebase/InAppMessaging'
    pod 'Firebase/Analytics'
    pod 'Firebase/Database'
  
end
